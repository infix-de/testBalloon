#!/bin/bash

ME="$(basename "$0")"

log() {
    echo -e "\n*** $(date +%X) – $ME: $* ***\n"
}

trap 'status=$?; log "completed with status $status"' EXIT
set -e

cd "$(dirname "$0")/.." || exit 2

log "starting"

REPORT_DIR="build/release-script-reports"
mkdir -p "$REPORT_DIR"
